# .NET
bin/
obj/
*.user
[Bb]uild/
[Tt]est[Rr]esults/
*.trx
*.coverage
*.nupkg

# Node (plugin tooling)
node_modules/

# Local / editor
.vs/
.idea/
*.swp
.DS_Store
Thumbs.db

# Claude Code local overrides — never shared
.claude/settings.local.json

# Secrets — nothing with a real value ever lands here (ADR 0007: Managed Identity only)
*.env
appsettings.*.Local.json
